name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Go
        uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Set up pnpm
        uses: pnpm/action-setup@f40ffcd9367d9f12939873eb1018b921a783ffaa # v4
        with:
          version: 10.29.3

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify Go
        run: |
          go vet ./...
          go test ./...

      - name: Cross-compile CLI
        run: |
          mkdir -p /tmp/lattice-builds
          CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -o /tmp/lattice-builds/lattice-darwin-arm64 ./cmd/lattice
          CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go build -o /tmp/lattice-builds/lattice-darwin-amd64 ./cmd/lattice
          CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -o /tmp/lattice-builds/lattice-linux-arm64 ./cmd/lattice
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o /tmp/lattice-builds/lattice-linux-amd64 ./cmd/lattice
          CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -o /tmp/lattice-builds/lattice-windows-amd64.exe ./cmd/lattice

      - name: Verify Worker
        run: pnpm --filter lattice-share typecheck

      - name: Build website
        run: pnpm --filter lattice-web build
